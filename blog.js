// Placeholder for static blog logic
document.addEventListener('DOMContentLoaded', function() {
  const blogSection = document.getElementById('blog-section');
  if (blogSection) {
    blogSection.innerHTML = '<h2>Blog posts are managed as static files. To add a post, create a new HTML or Markdown file in your repository.' +
      '<br>For comments, a free static comment system will be added soon.</h2>';
  }
});
